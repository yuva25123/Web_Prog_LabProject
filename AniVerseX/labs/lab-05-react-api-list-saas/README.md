# Lab 05: React API / List / SaaS

This folder contains the React side of AniVerseX.

Current contents:
- `frontend/` contains the moved React application.

Manual note:
- `frontend/` contains both a normal `.gitignore` file and an actual hidden `.git` folder. The nested `.git` was not changed and should be reviewed manually.
