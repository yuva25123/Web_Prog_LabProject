# Lab 07: Node.js and Express MVC

This folder contains the Node.js backend for AniVerseX.

Current contents:
- `backend/` contains the moved Express and MongoDB project files.

Status:
- Backend files are now separated from the React module.
