# Lab 03: JavaScript Events and Objects

This folder contains the current AniVerseX static review app.

Current contents:
- `app/` contains the moved static HTML, CSS, JavaScript, images, and related document for the events and objects lab.

Reason for mapping:
- The app uses DOM events, JavaScript objects, arrays, rendering logic, and form interaction.

Cleanup note:
- Asset folder names were normalized to lowercase to match the paths already used in the code.
