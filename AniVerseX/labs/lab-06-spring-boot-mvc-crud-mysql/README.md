# Lab 06: JSP / Spring Boot MVC CRUD with MySQL

This folder contains the Spring Boot portion of AniVerseX.

Current contents:
- `project/` contains the moved Maven project.

Status:
- Structure is now aligned to the lab list.
- The code still needs full MVC CRUD completion later.
